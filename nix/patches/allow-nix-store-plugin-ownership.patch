--- a/src/plugins/discovery.ts
+++ b/src/plugins/discovery.ts
@@ -30,5 +30,8 @@
 import type { PluginCandidate, PluginDiscoveryResult } from "./discovery.types.js";
-import { shouldRejectHardlinkedPluginFiles } from "./hardlink-policy.js";
+import {
+  isTrustedNixStorePluginRoot,
+  shouldRejectHardlinkedPluginFiles,
+} from "./hardlink-policy.js";
 import { hashStableJson } from "./installed-plugin-index-hash.js";
 import { readLegacyNpmPluginDeclaration } from "./legacy-npm-declaration.js";
 import type { PluginBundleFormat, PluginDiagnostic, PluginFormat } from "./manifest-types.js";
@@ -132,6 +135,7 @@
 }

 function checkPathStatAndPermissions(params: {
+  env: NodeJS.ProcessEnv;
   source: string;
   rootDir: string;
   origin: PluginOrigin;
@@ -191,6 +195,7 @@
     if (
       params.origin !== "bundled" &&
       params.uid !== null &&
+      !isTrustedNixStorePluginRoot(params) &&
       typeof stat.uid === "number" &&
       stat.uid !== params.uid &&
       stat.uid !== 0
@@ -209,6 +214,7 @@
 }

 function findCandidateBlockIssue(params: {
+  env: NodeJS.ProcessEnv;
   source: string;
   rootDir: string;
   origin: PluginOrigin;
@@ -222,6 +228,7 @@
     return escaped;
   }
   return checkPathStatAndPermissions({
+    env: params.env,
     source: params.source,
     rootDir: params.rootDir,
     origin: params.origin,
@@ -243,6 +250,7 @@
 }

 function isUnsafePluginCandidate(params: {
+  env: NodeJS.ProcessEnv;
   source: string;
   rootDir: string;
   origin: PluginOrigin;
@@ -251,6 +259,7 @@
   ownershipUid?: number | null;
 }): boolean {
   const issue = findCandidateBlockIssue({
+    env: params.env,
     source: params.source,
     rootDir: params.rootDir,
     origin: params.origin,
@@ -828,6 +837,7 @@
     const resolvedRoot = pluginCacheRealpathSync(params.rootDir) ?? path.resolve(params.rootDir);
     if (
       isUnsafePluginCandidate({
+        env,
         source: resolved,
         rootDir: resolvedRoot,
         origin: params.origin,
--- a/src/plugins/hardlink-policy.ts
+++ b/src/plugins/hardlink-policy.ts
@@ -21,6 +21,13 @@
   return rootRealPath === NIX_STORE_ROOT || rootRealPath.startsWith(`${NIX_STORE_ROOT}/`);
 }

+export function isTrustedNixStorePluginRoot(params: {
+  rootDir: string;
+  env?: NodeJS.ProcessEnv;
+}): boolean {
+  return resolveIsNixMode(params.env) && isNixStorePluginRoot(params.rootDir);
+}
+
 /** Decides whether plugin file hardlinks should fail boundary validation for one root. */
 export function shouldRejectHardlinkedPluginFiles(params: {
   origin: PluginOrigin;
@@ -31,7 +38,7 @@
   if (params.origin === "bundled") {
     return false;
   }
-  if (resolveIsNixMode(params.env) && isNixStorePluginRoot(params.rootDir)) {
+  if (isTrustedNixStorePluginRoot(params)) {
     return false;
   }
   return true;
diff --git a/src/plugins/package-entry-resolution.ts b/src/plugins/package-entry-resolution.ts
index e4e5aae..4ee8f11 100644
--- a/src/plugins/package-entry-resolution.ts
+++ b/src/plugins/package-entry-resolution.ts
@@ -13,2 +13,3 @@
+import { isTrustedNixStorePluginRoot } from "./hardlink-policy.js";
 import { checkPluginCacheEntry, pluginCacheExistsSync } from "./plugin-cache-files.js";
 import type { PluginOrigin } from "./plugin-origin.types.js";
@@ -117,6 +118,7 @@ async function validatePackageExtensionEntry(params: {
     absolutePath,
     rootPath: params.packageDir,
     boundaryLabel: "plugin package directory",
+    rejectHardlinks: !isTrustedNixStorePluginRoot({ rootDir: params.packageDir, env: process.env }),
   });
   if (!opened.ok) {
     return matchRootFileOpenFailure(opened, {
